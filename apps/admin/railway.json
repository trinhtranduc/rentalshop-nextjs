{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/admin/Dockerfile"
  },
  "deploy": {
    "startCommand": "cd apps/admin && ../../node_modules/.bin/next start -p 3001",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "environments": {
    "development": {
      "deploy": {
        "startCommand": "cd apps/admin && yarn dev",
        "healthcheckPath": "/",
        "healthcheckTimeout": 100
      }
    },
    "production": {
      "deploy": {
        "startCommand": "cd apps/admin && ../../node_modules/.bin/next start -p 3001",
        "healthcheckPath": "/",
        "healthcheckTimeout": 300,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
      }
    }
  }
}